logs:
  configs:
  - name: default
    clients:
      - url: http://loki:3100/loki/api/v1/push
    positions:
      filename: /tmp/positions.yaml
    scrape_configs:
      - job_name: gpts-api-transfer-open
        static_configs:
          - targets: [localhost]
            labels:
              job: gpts-api-transfer-open
              __path__: /var/log/gpts/*.log