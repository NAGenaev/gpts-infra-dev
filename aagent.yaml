logs:
  configs:
  - name: default
    clients:
      - url: http://loki:3100/loki/api/v1/push
    positions:
      filename: /tmp/positions.yaml
    scrape_configs:
      - job_name: gpts-publisher-dx
        static_configs:
          - targets: [localhost]
            labels:
              job: gpts-publisher-dx
              __path__: /var/log/gpts/*.log